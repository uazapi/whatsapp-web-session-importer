{
  "name": "whatsapp-web-session-importer",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build:vendor": "node scripts/build-vendor.mjs",
    "build:extension": "node scripts/build-extension.mjs",
    "build": "npm run build:vendor && npm run build:extension",
    "dist": "npm run build",
    "zip:dist": "rm -f whatsapp-web-session-importer.zip && npm run dist && cd dist && zip -qr ../whatsapp-web-session-importer.zip .",
    "zip": "npm run typecheck && npm test && npm run zip:dist",
    "release": "npm run zip"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "wa-store-migrate": "0.1.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
